fn main() {

    /*
        String - A dynamic pice of text sotred on the hheap at runtime

        &String ("ref String") - A reference to a heap string

        str - A hardoced, read-only piece of text encoded in the binary

        &str ("ref str") - A reference to the text in the memory tha has loaded the binary file
    
     */

    let ice_cream = "Cookies and Cream";
    println!("{}", ice_cream);
    
}
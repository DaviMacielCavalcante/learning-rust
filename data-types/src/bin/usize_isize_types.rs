fn main() {
    // usize, unsgined que varia de acordo com a arquitetura da máquina em que o código será executado
    let days: usize = 55;
    // isize é essencialmente as mesma coisa
    let years: isize = -15_000;
}